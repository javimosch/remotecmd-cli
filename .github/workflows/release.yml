name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    name: Build and Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Build (linux/amd64)
        run: GOOS=linux GOARCH=amd64 go build -ldflags "-s -w" -o remotecmd-cli-linux-amd64 .

      - name: Build (linux/arm64)
        run: GOOS=linux GOARCH=arm64 go build -ldflags "-s -w" -o remotecmd-cli-linux-arm64 .

      - name: Build (darwin/amd64)
        run: GOOS=darwin GOARCH=amd64 go build -ldflags "-s -w" -o remotecmd-cli-darwin-amd64 .

      - name: Build (darwin/arm64)
        run: GOOS=darwin GOARCH=arm64 go build -ldflags "-s -w" -o remotecmd-cli-darwin-arm64 .

      - name: Checksums
        run: sha256sum remotecmd-cli-* > checksums.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            remotecmd-cli-linux-amd64
            remotecmd-cli-linux-arm64
            remotecmd-cli-darwin-amd64
            remotecmd-cli-darwin-arm64
            checksums.txt
          fail_on_unmatched_files: true
